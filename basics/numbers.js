let age = 32
let dogYears = (age + 1) / 7

console.log(dogYears);

// challenge area
  // student score
  // max score
  // percent - final

  let studentScore = 41
  let maxScore = 100

  let percent = (studentScore / maxScore) * 100;
  console.log(percent);
