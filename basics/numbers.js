let age = 32
let dogYears = (age + 1) / 7

console.log(dogYears);

// challenge area
  // student score
  // max score
  // percent - final

  let studentScore = 70
  let maxScore = 75

  let percent = (studentScore / maxScore) * 100;
  console.log(percent);
  